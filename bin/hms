#!/usr/bin/env bash
# with-packages nixUnstable
tmpdir=$(mktemp -d)
result="$tmpdir"/result
cd ~/cfg
git add . -N
nix build .#homeConfigurations.$HOSTNAME \
  --out-link "$result" && "$result"/activate
rm -f "$result"
rmdir "$tmpdir"
