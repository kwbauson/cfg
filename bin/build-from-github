#!/usr/bin/env bash
# with-packages nix-wrapped cachix
set -x
output=$1
rev=$2
link=/tmp/$output:$rev
nix build --tarball-ttl 0 -o $link github:kwbauson/cfg?rev=$rev#$output
status=$?
cachix push kwbauson $link
unlink $link
exit $status
