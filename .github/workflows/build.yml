name: Build
on: [workflow_dispatch, push]
jobs:
  build:
    runs-on: self-hosted
    env:
      CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
    steps:
      - run: nix -vL build github:${{ github.repository }}/${{ github.sha }}
      - run: cachix push kwbauson result
