name: Build
on: [push, workflow_dispatch]
jobs:
  build:
    name: ${{ matrix.output }}
    strategy:
      fail-fast: false
      matrix:
        output:
          - nixosConfigurations.keith-xps.drv
          - nixosConfigurations.kwbauson.drv
          - nixosConfigurations.keith-vm.drv
          - homeConfigurations.graphical
          - homeConfigurations.non-graphical
    runs-on: ubuntu-20.04
    steps:
      - name: build on kwbauson.com
        run: |
          echo "${{ secrets.PRIVATE_KEY }}" > key && chmod 600 key
          ssh -i key -o StrictHostKeyChecking=no keith@kwbauson.com build-from-github ${{ github.sha }} ${{ matrix.output }}
